2026년 기준, 에이전트 시스템의

**동시 처리 건수(Concurrency/Throughput)**를 계산하기 위해서는 GPU의 **VRAM 용량**과 **연산 성능(TFLOPS)** 두 가지 측면을 모두 고려해야 합니다. 

가장 결정적인 제한 요소는 **VRAM(메모리)**입니다. 공식은 다음과 같습니다. 

* * *

1. VRAM 기반 동시 처리 건수 계산 (최대 수용량) 

GPU 메모리에 모델을 올리고 남은 공간에 몇 개의 대화(세션)를 담을 수 있는지 계산하는 방식입니다. 

**[공식]**

![](data:image/gif;base64,R0lGODlhAQABAIAAAP///wAAACH5BAEAAAAALAAAAAABAAEAAAICRAEAOw==)

동시 처리 가능 건수(Batch Size)=(총 VRAM−모델 가중치 크기−시스템 예약분) / (사용자 1인당 KV 캐시 점유량)

* **모델 가중치 크기:** 위에서 계산한 값 (예: Llama-3 8B 4-bit ≈ 5.5GB)
  
* **시스템 예약분:** CUDA 및 OS 점유량 (약 1~2GB)
  
* **1인당 KV 캐시 점유량:** 에이전트가 사용하는 **컨텍스트 길이(Tokens)**에 비례합니다.
  
  * _계산 팁:_ 보통 FP16 정밀도 기준, 7B~~8B 모델이 8K(8,192) 토큰의 컨텍스트를 유지할 때 **인당 약 1GB**~~2GB를 소모합니다. 

**[예시] RTX 4090 (24GB) 사용 시** 

* 모델(8B 4-bit): 6GB
  
* 시스템 예약: 2GB
  
* 남은 용량: 24−6−2=16𝐺𝐵
  
* 인당 KV 캐시(8K 토큰 설정): 약 1.5GB 가정 시
  
* **결과:** 16/1.5≈**약 10~11건 동시 처리 가능** 
  

* * *

2. 성능 기반 처리 속도 계산 (Throughput) 

메모리가 충분하더라도 GPU의 연산 속도가 느리면 동시 처리가 무의미해집니다. 이때는 **TPS(Tokens Per Second)** 단위를 사용합니다. 

* **총 TPS:** GPU가 1초에 생성할 수 있는 총 토큰 수 (하드웨어 성능)
  
* **사용자 체감 TPS:** (총 TPS) / (동시 접속자 수)
  
* **비교 기준:** 에이전트가 실시간으로 동작하려면 사용자당 최소 **15~20 TPS**는 유지되어야 합니다. 
  

* * *

3. 에이전트 시스템 설계 시 필수 고려 단위 

에이전트의 동시 처리를 비교·계산할 때 다음 지표를 리포트에 포함하세요. 

1. **Concurrency (동시성):** 동일 시점에 GPU 메모리에 상주하고 있는 요청 수.
  
2. **Requests Per Second (RPS):** 시스템이 초당 완료할 수 있는 총 에이전트 작업 수.
  
3. **Time To First Token (TTFT):** 에이전트가 생각을 시작하여 첫 번째 응답을 내놓을 때까지의 지연 시간 (초).
  
4. **Inference Engine:** 어떤 엔진을 쓰느냐에 따라 동시 처리량이 2~4배 차이 납니다. (예: vLLM, TGI는 **Paged Attention** 기술을 사용하여 KV 캐시 낭비를 줄여 동시 처리 건수를 극대화합니다.) 
  
5. 실전 추천 도구 
  

직접 계산이 어렵다면 [**vLLM의 가이드라인**](https://docs.vllm.ai/en/latest/models/engine_args.html)을 참고하십시오. 2026년 현재 vLLM은 `max_num_seqs` 옵션을 통해 GPU 메모리 상황에 맞는 최적의 동시 처리 건수를 자동으로 계산하여 할당해 줍니다. 

**결론적으로,** 에이전트 시스템의 처리 용량을 늘리려면 **VRAM이 큰 GPU**를 선택하거나, **vLLM 같은 서빙 프레임워크**를 사용하여 KV 캐시 효율을 높이는 것이 핵심입니다.